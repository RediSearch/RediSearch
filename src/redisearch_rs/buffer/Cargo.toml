[package]
name = "buffer"
version.workspace = true
edition.workspace = true
license-file.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
ffi.workspace = true

[features]
# Feature to enable tests that intentionally leak memory. We don't want to run these under miri
# and sanitizer tests
leaky_tests = []
