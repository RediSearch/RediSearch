#pragma once

/* WARNING: this file was auto-generated by cheadergen from `c_entrypoint/disk_write_buffer_ffi`. Do not edit it manually. */

#include <stdarg.h>
#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>

/**
 * Per-index disk write-buffer state owned by the Rust budget manager.
 *
 * C embeds this in `IndexSpec` and zero-initializes it with the rest of the spec.
 */
typedef struct DiskWriteBufferIndexState {
  bool hot;
  uint64_t hot_until_ms;
  size_t budget_epoch;
  size_t applied_budget;
} DiskWriteBufferIndexState;

#ifdef __cplusplus
extern "C" {
#endif // __cplusplus

/**
 * Return whether Redis has provided a non-zero Search disk write-buffer budget.
 */
bool DiskWriteBuffer_HasModuleBudget(void);

/**
 * Return the index's new budget after refreshing hot/cold state, if changed.
 *
 * # Safety
 *
 * `state` must be null or a valid, uniquely borrowed [`DiskWriteBufferIndexState`].
 */
size_t DiskWriteBuffer_MaintainIndex(struct DiskWriteBufferIndexState *state, uint64_t now_ms);

/**
 * Mark a disk index as actively receiving writes and return its new budget, if changed.
 *
 * # Safety
 *
 * `state` must be null or a valid, uniquely borrowed [`DiskWriteBufferIndexState`].
 */
size_t DiskWriteBuffer_MarkIndexWriteActive(struct DiskWriteBufferIndexState *state, uint64_t now_ms);

/**
 * Unregister a disk index from the write-buffer budget manager.
 *
 * # Safety
 *
 * `state` must be null or a valid, uniquely borrowed [`DiskWriteBufferIndexState`].
 */
void DiskWriteBuffer_RegisterIndexClose(struct DiskWriteBufferIndexState *state);

/**
 * Register a newly opened disk index with the write-buffer budget manager.
 *
 * # Safety
 *
 * `state` must be null or a valid, uniquely borrowed [`DiskWriteBufferIndexState`].
 */
void DiskWriteBuffer_RegisterIndexOpen(struct DiskWriteBufferIndexState *state, uint64_t now_ms);

/**
 * Set the total Search disk write-buffer budget.
 */
void DiskWriteBuffer_SetModuleBudget(size_t budget);

#ifdef __cplusplus
}  // extern "C"
#endif  // __cplusplus
