#pragma once

/* WARNING: this file was auto-generated by cheadergen from `query_types`. Do not edit it manually. */

#include <stdarg.h>
#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>
#include "rqe_core.h"
/* Backward-compatible aliases for C code that uses the old enum constant names. */
#define QN_PHRASE          QueryNodeType_Phrase
#define QN_UNION           QueryNodeType_Union
#define QN_TOKEN           QueryNodeType_Token
#define QN_NUMERIC         QueryNodeType_Numeric
#define QN_NOT             QueryNodeType_Not
#define QN_OPTIONAL        QueryNodeType_Optional
#define QN_GEO             QueryNodeType_Geo
#define QN_GEOMETRY        QueryNodeType_Geometry
#define QN_PREFIX          QueryNodeType_Prefix
#define QN_IDS             QueryNodeType_Ids
#define QN_WILDCARD        QueryNodeType_Wildcard
#define QN_TAG             QueryNodeType_Tag
#define QN_FUZZY           QueryNodeType_Fuzzy
#define QN_LEXRANGE        QueryNodeType_LexRange
#define QN_VECTOR          QueryNodeType_Vector
#define QN_WILDCARD_QUERY  QueryNodeType_WildcardQuery
#define QN_NULL            QueryNodeType_Null
#define QN_MISSING         QueryNodeType_Missing
#define QN_MAX             QueryNodeType_Max

/* Backward-compatible aliases for C code that uses the old enum constant names. */
#define QueryNode_Verbatim                   QueryNodeFlags_Verbatim
#define QueryNode_OverriddenInOrder          QueryNodeFlags_OverriddenInOrder
#define QueryNode_YieldsDistance              QueryNodeFlags_YieldsDistance
#define QueryNode_IndexesEmpty               QueryNodeFlags_IndexesEmpty
#define QueryNode_IsTag                      QueryNodeFlags_IsTag
#define QueryNode_HybridVectorSubqueryNode   QueryNodeFlags_HybridVectorSubqueryNode
#define QueryNode_HideVectorDistanceField    QueryNodeFlags_HideVectorDistanceField

/* Backward-compatible aliases for C code that uses the old enum constant names. */
#define QAST_SYNTAX_DEFAULT  0
#define QAST_NO_WEIGHT       QASTValidationFlags_NoWeight
#define QAST_NO_VECTOR       QASTValidationFlags_NoVector

/* Backward-compatible aliases for C code that uses the old PHONETIC_* macros. */
#define PHONETIC_DEFAULT  PhoneticMode_Default
#define PHONETIC_ENABLED  PhoneticMode_Enabled
#define PHONETIC_DISABLED PhoneticMode_Disabled


/**
 * The type of a query node.
 *
 * This enum is the single source of truth for query node type discriminants.
 * The C-side definition is generated by cheadergen from this Rust enum.
 */
enum QueryNodeType
#ifdef __cplusplus
  : uint32_t
#endif // __cplusplus
 {
  QueryNodeType_Phrase = 1,
  QueryNodeType_Union = 2,
  QueryNodeType_Token = 3,
  QueryNodeType_Numeric = 4,
  QueryNodeType_Not = 5,
  QueryNodeType_Optional = 6,
  QueryNodeType_Geo = 7,
  QueryNodeType_Geometry = 8,
  QueryNodeType_Prefix = 9,
  QueryNodeType_Ids = 10,
  QueryNodeType_Wildcard = 11,
  QueryNodeType_Tag = 12,
  QueryNodeType_Fuzzy = 13,
  QueryNodeType_LexRange = 14,
  QueryNodeType_Vector = 15,
  QueryNodeType_WildcardQuery = 16,
  QueryNodeType_Null = 17,
  QueryNodeType_Missing = 18,
  QueryNodeType_Max = 19,
};
#ifndef __cplusplus
typedef uint32_t QueryNodeType;
#endif // __cplusplus

/**
 * Per-AST flags stored in `QueryAST::validationFlags` (C struct defined in
 * `query.h`) that control which syntax features are allowed during query
 * validation (weight attributes, vector queries, etc.).
 *
 * This enum is the single source of truth for QAST validation flag bits.
 * The C-side definition is generated by cheadergen from this Rust enum.
 */
enum QASTValidationFlags
#ifdef __cplusplus
  : uint32_t
#endif // __cplusplus
 {
  /**
   * Weight attribute is not allowed.
   */
  QASTValidationFlags_NoWeight = 0x01,
  /**
   * Vector queries are not allowed.
   */
  QASTValidationFlags_NoVector = 0x02,
};
#ifndef __cplusplus
typedef uint32_t QASTValidationFlags;
#endif // __cplusplus

/**
 * Per-node flags stored in [`QueryNodeOptions::flags`] that control how
 * an individual query node is evaluated (verbatim matching, tag semantics,
 * vector distance handling, etc.).
 *
 * This enum is the single source of truth for query node flag bits.
 * The C-side definition is generated by cheadergen from this Rust enum.
 */
enum QueryNodeFlags
#ifdef __cplusplus
  : uint32_t
#endif // __cplusplus
 {
  /**
   * Prevents stemming, synonym expansion, and other query-time token
   * transformations. Set automatically for numeric/size literals and
   * for parameters that resolve to numeric values.
   */
  QueryNodeFlags_Verbatim = 0x01,
  /**
   * Indicates that the `inOrder` option was explicitly set via a query
   * attribute, so it should be checked even when not at the top level.
   */
  QueryNodeFlags_OverriddenInOrder = 0x02,
  /**
   * The node can produce a distance metric (e.g. vector similarity
   * score). Enables the `yield_distance_as` query attribute.
   */
  QueryNodeFlags_YieldsDistance = 0x04,
  /**
   * The tag field backing this node indexes empty values, so empty
   * documents should be matched. Only meaningful when [`Self::IsTag`] is set.
   */
  QueryNodeFlags_IndexesEmpty = 0x08,
  /**
   * The node is under a TAG index. Used together with [`Self::IndexesEmpty`]
   * to decide whether empty-value documents are included.
   */
  QueryNodeFlags_IsTag = 0x10,
  /**
   * Marks this as the main vector node in a hybrid vector subquery.
   */
  QueryNodeFlags_HybridVectorSubqueryNode = 0x20,
  /**
   * Suppresses the automatic distance field in the result set. Set when
   * no explicit `AS` alias is provided for the vector score field, so
   * the internally generated name is hidden from the user.
   */
  QueryNodeFlags_HideVectorDistanceField = 0x40,
};
#ifndef __cplusplus
typedef uint32_t QueryNodeFlags;
#endif // __cplusplus

/**
 * Phonetic matching mode for a query node.
 *
 * Controls whether phonetic expansion is applied when matching tokens
 * against indexed terms.
 *
 * This enum is the single source of truth for phonetic mode values.
 * The C-side definition is generated by cheadergen from this Rust enum.
 */
enum PhoneticMode
#ifdef __cplusplus
  : uint32_t
#endif // __cplusplus
 {
  /**
   * Defer to the field declaration: if the field was created with
   * phonetic matching, it is used; otherwise it is not.
   */
  PhoneticMode_Default = 0,
  /**
   * Force phonetic matching on, regardless of the field declaration.
   */
  PhoneticMode_Enabled = 1,
  /**
   * Force phonetic matching off, regardless of the field declaration.
   */
  PhoneticMode_Disabled = 2,
};
#ifndef __cplusplus
typedef uint32_t PhoneticMode;
#endif // __cplusplus

#ifndef BITFLAGS_QASTVALIDATIONFLAGS__U32_DEFINED
#define BITFLAGS_QASTVALIDATIONFLAGS__U32_DEFINED
/**
 * Represents a set of flags of some type `T`.
 * `T` must have the `#[bitflags]` attribute applied.
 *
 * A `BitFlags<T>` is as large as the `T` itself,
 * and stores one flag per bit.
 *
 * ## Comparison operators, [`PartialOrd`] and [`Ord`]
 *
 * To make it possible to use `BitFlags` as the key of a
 * [`BTreeMap`][std::collections::BTreeMap], `BitFlags` implements
 * [`Ord`]. There is no meaningful total order for bitflags,
 * so the implementation simply compares the integer values of the bits.
 *
 * Unfortunately, this means that comparing `BitFlags` with an operator
 * like `<=` will compile, and return values that are probably useless
 * and not what you expect. In particular, `<=` does *not* check whether
 * one value is a subset of the other. Use [`BitFlags::contains`] for that.
 *
 * ## Customizing `Default`
 *
 * By default, creating an instance of `BitFlags<T>` with `Default` will result
 * in an empty set. If that's undesirable, you may customize this:
 *
 * ```
 * # use enumflags2::{BitFlags, bitflags};
 * #[bitflags(default = B | C)]
 * #[repr(u8)]
 * #[derive(Copy, Clone, Debug, PartialEq)]
 * enum MyFlag {
 *     A = 0b0001,
 *     B = 0b0010,
 *     C = 0b0100,
 *     D = 0b1000,
 * }
 *
 * assert_eq!(BitFlags::default(), MyFlag::B | MyFlag::C);
 * ```
 *
 * ## Memory layout
 *
 * `BitFlags<T>` is marked with the `#[repr(transparent)]` trait, meaning
 * it can be safely transmuted into the corresponding numeric type.
 *
 * Usually, the same can be achieved by using [`BitFlags::bits`] in one
 * direction, and [`BitFlags::from_bits`], [`BitFlags::from_bits_truncate`],
 * or [`BitFlags::from_bits_unchecked`] in the other direction. However,
 * transmuting might still be useful if, for example, you're dealing with
 * an entire array of `BitFlags`.
 *
 * When transmuting *into* a `BitFlags`, make sure that each set bit
 * corresponds to an existing flag
 * (cf. [`from_bits_unchecked`][BitFlags::from_bits_unchecked]).
 *
 * For example:
 *
 * ```
 * # use enumflags2::{BitFlags, bitflags};
 * #[bitflags]
 * #[repr(u8)] // <-- the repr determines the numeric type
 * #[derive(Copy, Clone)]
 * enum TransmuteMe {
 *     One = 1 << 0,
 *     Two = 1 << 1,
 * }
 *
 * # use std::slice;
 * // NOTE: we use a small, self-contained function to handle the slice
 * // conversion to make sure the lifetimes are right.
 * fn transmute_slice<'a>(input: &'a [BitFlags<TransmuteMe>]) -> &'a [u8] {
 *     unsafe {
 *         slice::from_raw_parts(input.as_ptr() as *const u8, input.len())
 *     }
 * }
 *
 * let many_flags = &[
 *     TransmuteMe::One.into(),
 *     TransmuteMe::One | TransmuteMe::Two,
 * ];
 *
 * let as_nums = transmute_slice(many_flags);
 * assert_eq!(as_nums, &[0b01, 0b11]);
 * ```
 *
 * ## Implementation notes
 *
 * You might expect this struct to be defined as
 *
 * ```ignore
 * struct BitFlags<T: BitFlag> {
 *     value: T::Numeric
 * }
 * ```
 *
 * Ideally, that would be the case. However, because `const fn`s cannot
 * have trait bounds in current Rust, this would prevent us from providing
 * most `const fn` APIs. As a workaround, we define `BitFlags` with two
 * type parameters, with a default for the second one:
 *
 * ```ignore
 * struct BitFlags<T, N = <T as BitFlag>::Numeric> {
 *     value: N,
 *     marker: PhantomData<T>,
 * }
 * ```
 *
 * Manually providing a type for the `N` type parameter shouldn't ever
 * be necessary.
 *
 * The types substituted for `T` and `N` must always match, creating a
 * `BitFlags` value where that isn't the case is only possible with
 * incorrect unsafe code.
 */
typedef uint32_t BitFlags_QASTValidationFlags__u32;
#endif /* BITFLAGS_QASTVALIDATIONFLAGS__U32_DEFINED */

/**
 * Flags indicating which syntax features are enabled for a query AST.
 *
 * This enum is the single source of truth for QAST validation flag bits.
 * The C-side definition is generated by cheadergen from this Rust enum.
 */
typedef BitFlags_QASTValidationFlags__u32 QASTValidationFlagsSet;

#ifndef BITFLAGS_QUERYNODEFLAGS__U32_DEFINED
#define BITFLAGS_QUERYNODEFLAGS__U32_DEFINED
/**
 * Represents a set of flags of some type `T`.
 * `T` must have the `#[bitflags]` attribute applied.
 *
 * A `BitFlags<T>` is as large as the `T` itself,
 * and stores one flag per bit.
 *
 * ## Comparison operators, [`PartialOrd`] and [`Ord`]
 *
 * To make it possible to use `BitFlags` as the key of a
 * [`BTreeMap`][std::collections::BTreeMap], `BitFlags` implements
 * [`Ord`]. There is no meaningful total order for bitflags,
 * so the implementation simply compares the integer values of the bits.
 *
 * Unfortunately, this means that comparing `BitFlags` with an operator
 * like `<=` will compile, and return values that are probably useless
 * and not what you expect. In particular, `<=` does *not* check whether
 * one value is a subset of the other. Use [`BitFlags::contains`] for that.
 *
 * ## Customizing `Default`
 *
 * By default, creating an instance of `BitFlags<T>` with `Default` will result
 * in an empty set. If that's undesirable, you may customize this:
 *
 * ```
 * # use enumflags2::{BitFlags, bitflags};
 * #[bitflags(default = B | C)]
 * #[repr(u8)]
 * #[derive(Copy, Clone, Debug, PartialEq)]
 * enum MyFlag {
 *     A = 0b0001,
 *     B = 0b0010,
 *     C = 0b0100,
 *     D = 0b1000,
 * }
 *
 * assert_eq!(BitFlags::default(), MyFlag::B | MyFlag::C);
 * ```
 *
 * ## Memory layout
 *
 * `BitFlags<T>` is marked with the `#[repr(transparent)]` trait, meaning
 * it can be safely transmuted into the corresponding numeric type.
 *
 * Usually, the same can be achieved by using [`BitFlags::bits`] in one
 * direction, and [`BitFlags::from_bits`], [`BitFlags::from_bits_truncate`],
 * or [`BitFlags::from_bits_unchecked`] in the other direction. However,
 * transmuting might still be useful if, for example, you're dealing with
 * an entire array of `BitFlags`.
 *
 * When transmuting *into* a `BitFlags`, make sure that each set bit
 * corresponds to an existing flag
 * (cf. [`from_bits_unchecked`][BitFlags::from_bits_unchecked]).
 *
 * For example:
 *
 * ```
 * # use enumflags2::{BitFlags, bitflags};
 * #[bitflags]
 * #[repr(u8)] // <-- the repr determines the numeric type
 * #[derive(Copy, Clone)]
 * enum TransmuteMe {
 *     One = 1 << 0,
 *     Two = 1 << 1,
 * }
 *
 * # use std::slice;
 * // NOTE: we use a small, self-contained function to handle the slice
 * // conversion to make sure the lifetimes are right.
 * fn transmute_slice<'a>(input: &'a [BitFlags<TransmuteMe>]) -> &'a [u8] {
 *     unsafe {
 *         slice::from_raw_parts(input.as_ptr() as *const u8, input.len())
 *     }
 * }
 *
 * let many_flags = &[
 *     TransmuteMe::One.into(),
 *     TransmuteMe::One | TransmuteMe::Two,
 * ];
 *
 * let as_nums = transmute_slice(many_flags);
 * assert_eq!(as_nums, &[0b01, 0b11]);
 * ```
 *
 * ## Implementation notes
 *
 * You might expect this struct to be defined as
 *
 * ```ignore
 * struct BitFlags<T: BitFlag> {
 *     value: T::Numeric
 * }
 * ```
 *
 * Ideally, that would be the case. However, because `const fn`s cannot
 * have trait bounds in current Rust, this would prevent us from providing
 * most `const fn` APIs. As a workaround, we define `BitFlags` with two
 * type parameters, with a default for the second one:
 *
 * ```ignore
 * struct BitFlags<T, N = <T as BitFlag>::Numeric> {
 *     value: N,
 *     marker: PhantomData<T>,
 * }
 * ```
 *
 * Manually providing a type for the `N` type parameter shouldn't ever
 * be necessary.
 *
 * The types substituted for `T` and `N` must always match, creating a
 * `BitFlags` value where that isn't the case is only possible with
 * incorrect unsafe code.
 */
typedef uint32_t BitFlags_QueryNodeFlags__u32;
#endif /* BITFLAGS_QUERYNODEFLAGS__U32_DEFINED */

/**
 * Bitflags for per-node query options.
 *
 * This enum is the single source of truth for query node flag bits.
 * The C-side definition is generated by cheadergen from this Rust enum.
 */
typedef BitFlags_QueryNodeFlags__u32 QueryNodeFlagsSet;

/**
 * Various modifiers and options that can apply to the entire query or any
 * sub-query of it.
 */
typedef struct QueryNodeOptions {
  /**
   * Bitflags controlling evaluation behavior for this node.
   */
  QueryNodeFlagsSet flags;
  /**
   * Bitmask of fields this node matches against. Intersected with the
   * top-level search field mask at evaluation time. Defaults to
   * `RS_FIELDMASK_ALL`.
   */
  t_fieldMask fieldMask;
  /**
   * Index of the specific field this node targets (used by tag queries
   * to pass the field index to the tag iterator).
   */
  t_fieldIndex fieldIndex;
  /**
   * Maximum allowed slop (distance between terms) for phrase matching.
   * A value of `-1` means no local override (the top-level default
   * applies).
   */
  int32_t maxSlop;
  /**
   * Whether child terms must appear in order within the slop window.
   */
  int32_t inOrder;
  /**
   * Scoring weight multiplier for this node. Defaults to `1.0`.
   */
  double weight;
  /**
   * Phonetic matching mode for this node.
   */
  PhoneticMode phonetic;
  /**
   * Name of the distance field to emit in results (set via the
   * `yield_distance_as` query attribute on vector nodes). Owned
   * null-terminated C string, freed with `rm_free` when the query
   * node is destroyed. `NULL` when no distance field is requested.
   */
  char *distField;
  /**
   * Whether [`weight`](Self::weight) was explicitly set by the user in
   * the query (as opposed to the default of `1.0`).
   */
  bool explicitWeight;
} QueryNodeOptions;

/**
 * Default query expander (`DEFAULT`). Used when a query does not request one.
 */
#define DEFAULT_EXPANDER_NAME "DEFAULT"

/**
 * TF-IDF scorer (`TFIDF`). Uses term proximity, so it needs term offsets.
 */
#define TFIDF_SCORER_NAME "TFIDF"

/**
 * Phonetic-matching query expander (`PHONETIC`).
 */
#define PHONETIC_EXPENDER_NAME "PHONETIC"

/**
 * Document-normalized TF-IDF scorer (`TFIDF.DOCNORM`).
 */
#define TFIDF_DOCNORM_SCORER_NAME "TFIDF.DOCNORM"

/**
 * Synonym-expansion query expander (`SYNONYM`).
 */
#define SYNONYMS_EXPENDER_NAME "SYNONYM"

/**
 * Disjunction-max scorer (`DISMAX`).
 */
#define DISMAX_SCORER_NAME "DISMAX"

/**
 * Stemming query expander (`SBSTEM`).
 */
#define STEMMER_EXPENDER_NAME "SBSTEM"

/**
 * Legacy BM25 scorer (`BM25`).
 */
#define BM25_SCORER_NAME "BM25"

/**
 * Standard BM25 scorer (`BM25STD`). The default scorer.
 */
#define BM25_STD_SCORER_NAME "BM25STD"

/**
 * Standard BM25 scorer with tanh normalization (`BM25STD.TANH`).
 */
#define BM25_STD_NORMALIZED_TANH_SCORER_NAME "BM25STD.TANH"

/**
 * Standard BM25 scorer with max normalization (`BM25STD.NORM`).
 */
#define BM25_STD_NORMALIZED_MAX_SCORER_NAME "BM25STD.NORM"

/**
 * Document-score scorer (`DOCSCORE`).
 */
#define DOCSCORE_SCORER "DOCSCORE"

/**
 * Hamming-distance scorer (`HAMMING`).
 */
#define HAMMINGDISTANCE_SCORER "HAMMING"

/**
 * Name of the scorer used when a query does not request one explicitly.
 *
 * This is the standard BM25 scorer. It is spelled out as a literal, matching
 * [`BM25_STD_SCORER_NAME`], because the C header generator only exports literal
 * string constants.
 */
#define DEFAULT_SCORER_NAME "BM25STD"
