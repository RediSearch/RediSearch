#pragma once

/* Warning, this file is autogenerated by cbindgen from `src/redisearch_rs/c_entrypoint/reducers/build.rs. Don't modify it manually. */

#include <stdarg.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdlib.h>

#ifdef __cplusplus
extern "C" {
#endif // __cplusplus

/**
 * Creates a new counter reducer instance
 *
 * # Safety
 *
 * 1. `r` must point to a [valid] `ffi::Reducer`.
 *
 * [valid]: https://doc.rust-lang.org/std/ptr/index.html#safety
 */
void *counterNewInstance(Reducer *r);

/**
 * Frees a counter reducer instance
 *
 * # Safety
 *
 * 1. `r` must point to a [valid] `ffi::Reducer`.
 * 2. `ctx` mut point to a [valid] `Counter`.
 *
 * [valid]: https://doc.rust-lang.org/std/ptr/index.html#safety
 */
void counterFreeInstance(Reducer *r, void *ctx);

/**
 * Process the provided `RLookupRow` with the counter reducer instance.
 *
 * # Safety
 *
 * 1. `r` must point to a [valid] `ffi::Reducer`.
 * 2. `ctx` mut point to a [valid] `Counter`.
 * 3. `srcrow` mut point to a [valid] `ffi::RLookupRow`.
 *
 * [valid]: https://doc.rust-lang.org/std/ptr/index.html#safety
 */
int counterAdd(Reducer *r, void *ctx, const RLookupRow *srcrow);

/**
 * Finalize the counter reducer instance result into an `RSValue`.
 *
 * # Safety
 *
 * 1. `r` must point to a [valid] `ffi::Reducer`.
 * 2. `ctx` mut point to a [valid] `Counter`.
 *
 * [valid]: https://doc.rust-lang.org/std/ptr/index.html#safety
 */
RSValue *counterFinalize(Reducer *r, void *ctx);

#ifdef __cplusplus
}  // extern "C"
#endif  // __cplusplus
