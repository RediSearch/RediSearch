#ifndef value_h
#define value_h

/* Warning, this file is autogenerated by cbindgen from `src/redisearch_rs/c_entrypoint/value_ffi/build.rs. Don't modify it manually. */

#include <stdarg.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdlib.h>
#include "redismodule.h"
#include "query_error.h"

#define RSValueSize 16

/**
 * Enumeration of the types an
 * `RsValue` or a `SharedRsValue` can be of.
 */
typedef enum RSValueType {
  RSValueType_Undef = 0,
  RSValueType_Number = 1,
  RSValueType_String = 3,
  RSValueType_Null = 4,
  RSValueType_RedisString = 5,
  RSValueType_Array = 6,
  RSValueType_Reference = 8,
  RSValueType_Trio = 9,
  RSValueType_Map = 10,
} RSValueType;

/**
 * An actual [`RsValue`] object
 */
typedef struct RSValue RSValue;

/**
 * A single entry of a [`RsValueMap`].
 */
typedef struct RsValueMapEntry {
  struct RSValue * key;
  struct RSValue * value;
} RsValueMapEntry;

/**
 * An low-memory immutable structure that holds and manages a set of
 * T items.
 *
 * This collection's capacity is represented by an `u32` and the
 * collection itself is `#[repr(C, packed)]` so that it's size is just
 * 12 bytes.
 *
 * # Invariants
 * - (1) Can hold at most [`Self::MAX_CAPACITY`] items, which on 32-bit systems
 *   is less than `u32::MAX`. The reason for this is that when doing pointer
 *   addition, we must ensure we don't overflow `isize::MAX`.
 *   See [`NonNull::add`].
 */
typedef struct RsValueCollection_RsValueMapEntry {
  /**
   * Pointer to a heap-allocated array of `Self::cap` items.
   */
  struct RsValueMapEntry *entries;
  /**
   * The number of items this collection can hold
   */
  uint32_t cap;
} RsValueCollection_RsValueMapEntry;

typedef struct RsValueCollection_RsValueMapEntry RSValueMap;

#ifdef __cplusplus
extern "C" {
#endif // __cplusplus

struct RSValue **RSValue_AllocateArray(uintptr_t len);

struct RSValue *RSValue_NewArray(struct RSValue **vals, uint32_t len);

uint32_t RSValue_ArrayLen(const struct RSValue *value);

struct RSValue *RSValue_ArrayItem(const struct RSValue *value, uint32_t index);

struct RSValue *RSValue_NewUndefined(void);

struct RSValue *RSValue_NewNull(void);

struct RSValue *RSValue_NullStatic(void);

struct RSValue *RSValue_NewNumber(double value);

struct RSValue *RSValue_NewRedisString(const RedisModuleString *s);

struct RSValue *RSValue_NewCopiedString(const char *value, uint32_t len);

struct RSValue *RSValue_NewReference(const struct RSValue *src);

struct RSValue *RSValue_NewParsedNumber(const char *value, uint32_t len);

struct RSValue *RSValue_NewNumberFromInt64(int64_t ii);

struct RSValue *RSValue_NewTrio(struct RSValue *val,
                                struct RSValue *otherval,
                                struct RSValue *other2val);

RSValueMap RSValueMap_AllocUninit(uint32_t value);

struct RSValue *RSValue_NewMap(RSValueMap map);

void RSValueMap_SetEntry(RSValueMap *map, intptr_t i, struct RSValue *key, struct RSValue *value);

uint32_t RSValue_Map_Len(const struct RSValue *map);

void RSValue_Map_GetEntry(const struct RSValue *map,
                          uint32_t i,
                          struct RSValue **key,
                          struct RSValue **value);

struct RSValue *RSValue_Dereference(const struct RSValue *value);

void RSValue_Clear(const struct RSValue *value);

struct RSValue *RSValue_IncrRef(const struct RSValue *value);

void RSValue_DecrRef(const struct RSValue *value);

void RSValue_MakeReference(const struct RSValue *dst, const struct RSValue *src);

void RSValue_MakeOwnReference(const struct RSValue *dst, const struct RSValue *src);

void RSValue_Replace(struct RSValue **dstpp, const struct RSValue *src);

double RSValue_Number_Get(const struct RSValue *value);

uintptr_t RSValue_Refcount(const struct RSValue *value);

const struct RSValue *RSValue_Trio_GetLeft(const struct RSValue *value);

const struct RSValue *RSValue_Trio_GetMiddle(const struct RSValue *value);

const struct RSValue *RSValue_Trio_GetRight(const struct RSValue *value);

void RSValue_SetNumber(struct RSValue *value, double n);

void RSValue_IntoNull(struct RSValue *value);

const RedisModuleString *RSValue_RedisString_Get(const struct RSValue *value);

int RSValue_ToNumber(const struct RSValue *value, double *d);

void RSValue_IntoNumber(struct RSValue *value, double n);

const char *RSValue_ConvertStringPtrLen(const struct RSValue *v,
                                        size_t *lenp,
                                        char *buf,
                                        size_t buflen);

const char *RSValue_StringPtrLen(const struct RSValue *value, size_t *lenp);

int RSValue_Cmp(const struct RSValue *v1, const struct RSValue *v2, QueryError *status);

int RSValue_Equal(const struct RSValue *v1, const struct RSValue *v2, QueryError *status);

int RSValue_BoolTest(const struct RSValue *v);

uint64_t RSValue_Hash(const struct RSValue *v, uint64_t hval);

void RSValue_ToString(const struct RSValue *dst, const struct RSValue *value);

size_t RSValue_NumToString(const struct RSValue *v, char *buf, size_t buflen);

sds RSValue_DumpSds(const struct RSValue *value, sds sds, bool obfuscate);

sds RSValue_SetString(const struct RSValue *v, char *str, size_t len);

sds RSValue_SetConstString(const struct RSValue *v, const char *str, size_t len);

struct RSValue *RSValue_NewString(char *str, uint32_t len);

struct RSValue *RSValue_NewConstString(const char *str, uint32_t len);

char *RSValue_String_Get(struct RSValue *value, uint32_t *lenp);

/**
 * Get the type of an `RsValue`.
 *
 * # Safety
 * The passed value must originate from one of the `RsValue` constructors,
 * i.e. [`RsValue_Undefined`], [`RsValue_Number`], [`RsValue_String`],
 * or [`RsValue_NullStatic`].
 *
 * @param v The value to inspect
 * @return The `RsValueType` of the value
 */
enum RSValueType RSValue_Type(const struct RSValue *v);

bool RSValue_IsReference(const struct RSValue *value);

bool RSValue_IsNumber(const struct RSValue *value);

bool RSValue_IsString(const struct RSValue *value);

bool RSValue_IsArray(const struct RSValue *value);

bool RSValue_IsRedisString(const struct RSValue *value);

bool RSValue_IsTrio(const struct RSValue *value);

bool RSValue_IsAnyString(const struct RSValue *value);

bool RSValue_IsNull(const struct RSValue *value);

#ifdef __cplusplus
}  // extern "C"
#endif  // __cplusplus

#endif  /* value_h */
