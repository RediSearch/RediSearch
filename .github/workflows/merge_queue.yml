name: Merge pull request

on:
  merge_group:
    types: [checks_requested]

jobs:
  run-flow:
    uses: ./.github/workflows/merge-nightly-common.yml
    secrets: inherit
    with:
      redis-ref: latest version # TODO: get somehow (from env, from repo, hardcode)
      json-ref: latest version # TODO: get somehow (from env, from repo, hardcode)
