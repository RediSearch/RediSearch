name: temporary testing

on:
  push:
    # branches-ignore: ['*'] # ignore all branches

jobs:
  # test-no-ref:
  #   uses: ./.github/workflows/flow-build-artifacts.yml
  #   with:
  #     architecture: x86_64
  #     platform: ubuntu:jammy

  test-branch-ref:
    uses: ./.github/workflows/flow-build-artifacts.yml
    with:
      architecture: x86_64
      platform: ubuntu:jammy

      reference: master

  test-tag-ref:
    uses: ./.github/workflows/flow-build-artifacts.yml
    with:
      architecture: x86_64
      platform: ubuntu:jammy

      reference: v2.8.8

  test-sha-ref:
    uses: ./.github/workflows/flow-build-artifacts.yml
    with:
      architecture: x86_64
      platform: ubuntu:jammy

      reference: 921bf8019c

  test-bad-ref:
    uses: ./.github/workflows/flow-build-artifacts.yml
    with:
      architecture: x86_64
      platform: ubuntu:jammy

      reference: bad-ref

  test-checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.sha }}
