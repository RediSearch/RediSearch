name: temporary testing

on:
  push:
    # branches-ignore: ['*'] # ignore all branches

jobs:
  # test-no-ref:
  #   uses: ./.github/workflows/flow-build-artifacts.yml
  #   with:
  #     architecture: x86_64
  #     platform: ubuntu:jammy

  # test-branch-ref:
  #   uses: ./.github/workflows/flow-build-artifacts.yml
  #   with:
  #     architecture: x86_64
  #     platform: ubuntu:jammy

  #     reference: master

  # test-tag-ref:
  #   uses: ./.github/workflows/flow-build-artifacts.yml
  #   with:
  #     architecture: x86_64
  #     platform: ubuntu:jammy

  #     reference: v2.8.8

  test-short-sha-ref:
    uses: ./.github/workflows/flow-build-artifacts.yml
    with:
      architecture: x86_64
      platform: ubuntu:jammy

      reference: 921bf8019c

  test-long-sha-ref:
    uses: ./.github/workflows/flow-build-artifacts.yml
    with:
      architecture: x86_64
      platform: ubuntu:jammy

      reference: 921bf8019c2e8d94648fa38593d0141c1064261e

  # test-bad-ref:
  #   uses: ./.github/workflows/flow-build-artifacts.yml
  #   with:
  #     architecture: x86_64
  #     platform: ubuntu:jammy

  #     reference: bad-ref
