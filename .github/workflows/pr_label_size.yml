name: labeler

on: [pull_request]

# Grant the token the reusable workflow needs (it labels the PR).
permissions:
  contents: read
  pull-requests: write

jobs:
  labeler:
    uses: redislabsdev/redisearch-ci-common/.github/workflows/pr-size-label.yml@v1
