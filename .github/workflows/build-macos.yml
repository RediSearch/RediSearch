name: Build on macOS

on:
  push:
  workflow_dispatch:

jobs:
  build-macos:
    uses: ./.github/workflows/common-flow-host.yml
    with:
      env: macos-11
      get-redis: true
    secrets: inherit
