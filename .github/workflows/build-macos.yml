name: Build on macOS

on:
  workflow_dispatch:
  push:
    branches-ignore:
      - '*docs'
      - '*noci'

jobs:
  build-macos:
    uses: ./.github/workflows/common-flow-host.yml
    with:
      env: macos-11
      get-redis: true
    secrets: inherit
