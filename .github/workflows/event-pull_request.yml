name: Pull Request Flow

on:
  pull_request:
    branches:
      - codex/mq-master-mock
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

jobs:
  pr-validation:
    runs-on: ubuntu-slim
    steps:
      - run: echo "Mock PR validation passed"
