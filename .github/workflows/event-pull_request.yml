name: Pull Request Flow

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pr-validation:
    runs-on: ubuntu-slim
    steps:
      - name: Pass mock pull request validation
        run: echo "Mock PR validation passed"
