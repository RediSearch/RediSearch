coverage:
  status:
    project:
      default:
        # basic
        #target: 80%
        #threshold: 1%
        base: auto 
        paths: 
          - "src"
          - "coord/src"
       # advanced
       # branches: 
       #   - master
        if_not_found: success
        if_ci_failed: error
        informational: True
        only_pulls: false