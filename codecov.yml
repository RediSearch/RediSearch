coverage:
  status:
    project:
      default:
        # basic
        target: 80%
        threshold: 1%
        base: auto 
        #flags: 
        #  - unit
        paths: 
          - "src"
       # advanced
       # branches: 
       #   - master
        if_not_found: success
        if_ci_failed: error
        informational: False
        only_pulls: false