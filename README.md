# RediSearch for Enterprise

An extension for RediSearch, which contains the private content that belongs to Enterprise only, and not to Redis Open Source, including index on disk capabilities.

Artifacts for Redis Enterprise will be built and deployed from this repo starting with **version 8.6**.
